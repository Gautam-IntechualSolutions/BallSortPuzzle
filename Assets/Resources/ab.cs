using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ab : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //var squarePos= new Vector2(squareBox.transform.position.x, squareBox.transform.position.y+2.25f); ;
        //temp=Instantiate(outBox,squarePos,Quaternion.identity);

        //Debug.Log(outBox.transform.position);
        //for (i = 0; i < 4; i++)
        //{
        //    var curPos =new Vector2(squareBox.transform.GetChild(i).transform.position.x, squareBox.transform.GetChild(i).transform.position.y);
        //    circleList2.Add(Instantiate(circleList[Random.Range(1,7)],curPos,Quaternion.identity));
        //}
    }

    // Update is called once per frame
    void Update()
    {
        //var outPos = new Vector2(temp.transform.GetChild(5).transform.localPosition, temp.transform.position.y);
        //Debug.Log("Out Box Position " +outPos+" Last Circle in List " +circleList2[listLength - 1].name);
        ////circleList2[listLength - 1].transform.Translate(new Vector2(outPos.x, outPos.y));
        //circleList2[listLength - 1].transform.Translate(new Vector2(outPos.x, outPos.y));

        //outBox = circleList2[listLength - 1]; 
        //Destroy(circleList2[listLength - 1]);

    }
}
